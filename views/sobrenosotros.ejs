<%- include("templates/head", {titulo: "Sobre nosotros | wetapas"}) %> 
<link rel="stylesheet" href="../public/css/views/sobrenosotros.css">
</head>
<body>
   
    <% if(locals.totalItems && locals.user) {%>
    <%- include("templates/cabecera", {user, totalItems}) %> 
    <% } else if(locals.user) {%>
        <%- include("templates/cabecera", {user}) %> 
    <% } else if(locals.totalItems){ %>
        <%- include("templates/cabecera", {totalItems}) %>
    <% } else if (!locals.totalItems && !locals.user) { %>
        <%- include("templates/cabecera") %> 
    <% } %>
    
    <main class="main">
        <div class="container">
            <div class="row my-4 gap-4 pt-0">
                <section class="col-12">

                    <div class="sobreNosotros-section">

                        <h1 class="title text-center">¿Cansado de no saber qué poner a tus invitados?</h1>
        
                        <p class="text-paragraph">Esta empresa nace para cubrir esa necesidad con el objetivo de disfrutar de nuestras artesanales tapas en todas las casas al alcance de todos los bolsillos y con tan solo un click<p>

                    </div>

                </section>
                <section class="col-12" id="contacto">
                    <div class="sobreNosotros-section">
                        <h2 class="title text-center">¿Cómo contactar con nosotros?</h2>
                        <div class="btn-section">
                            <a href="mailto:icarluus@outlook.com" class="btn">Email</a>
                            <a href="tel:+34612123123" class="btn">Telefono</a>
                            <a href="#" class="btn">Twitter</a>
                            <a href="#" class="btn">Instagram</a>
                            <a href="#" class="btn">Facebook</a>
                            
                        </div>
                    </div>
                        <!-- <form action="/contacto" method="post">

                        </form> -->
                </section>
            </div>
        </div>
    </main>



    <%- include("templates/footer") %> 
    <script src="../../public/js/menu-cabecera.js"></script>
    <script src="../public/js/alerts.js"></script>
</body>